<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Electron/V8 need writable+executable memory and JIT. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <!-- Electron sets DYLD_* for its helper processes. -->
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <!-- The app spawns the user's `hermes` binary (and its Python runtime),
       which loads dylibs signed by other teams / unsigned. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <!-- Talks to the local Hermes dashboard over loopback and fetches updates. -->
  <key>com.apple.security.network.client</key>
  <true/>
</dict>
</plist>

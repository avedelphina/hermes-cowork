packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 blocks lifecycle scripts unless the package is listed here.
# electron's postinstall downloads the platform binary; without it the app
# cannot launch. esbuild's postinstall installs its native binary.
onlyBuiltDependencies:
  - electron
  - esbuild

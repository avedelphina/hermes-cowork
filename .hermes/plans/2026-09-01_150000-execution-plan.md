# Hermes Cowork — Execution Plan

Baseline: 37 tests pass, typecheck clean. Roadmap: `2026-09-01_140500-hermes-cowork-roadmap.md`.

## Bugs found in review (mapped to roadmap tasks)

1. **HERMES_HOME double-scope** — `index.ts` sets `defaultHermesHome=activeHermesHome=$HERMES_HOME`; `handlers.ts` appends `/profiles/<name>`, giving `.../profiles/anikke/profiles/anikke` when env is already profile-scoped. (Task 1.1)
2. **Cowork composer no-op** — `CoworkPage` reuses chat `Composer` reading `useChatStore.sessionId` (null in Cowork). Steering dead. (Task 3.2)
3. **Plan/approval renderer-only** — `approvalMode` never sent to main; `upsertPlanTask`/`setParent` never called; PlanTab always empty. (Task 3.3/3.4)
4. **Kanban WS unscoped** — pump broadcasts all events; `KanbanWsSubscribe` is a stub. (Task 3.4)
5. **Artifact detection dead** — store matches `msg.name==='write_file'|'patch'`, translator sets `name` from `update.title`. (Task 3.5)
6. **ACP crash silent** — translator drops `exit`/`error`; bridge only handles `kind==='message'`. (Task 4.4)
7. **Dashboard child leaks on quit** — `before-quit` never kills spawned `hermes dashboard`; fixed port 9119 no fallback. (Task 1.4)
8. **No project layer / no cwd validation / no security-model.md** (Task 0.3, Phase 2)
9. **No task persistence** — `cowork.store` in-memory; `startTask` wipes state; no resume. (Task 3.1)

## Roadmap doc fixes
- Phase 5: duplicate "Task 5.3" numbers; renumber 5.6/5.7.
- Phase 5 assumes `kanban_create --assignee` spawns isolated profiles — unverified. Add spike before 5.1.
- Version mismatch: `MIN_HERMES_VERSION` 0.13.0 vs target 0.20.6 vs ACP v0.11.2 comments. Reconcile in Task 1.3.

## Execution order

- [x] **0.1** install repro: pnpm config warning, pin Node/pnpm, remove manual Electron-repair step — `c3977b4`
- [x] **1.1 + 1.2** HERMES_HOME resolution + truthful profile state — `e7e8323`
- [x] **1.3** exercise ACP vs real Hermes 0.20.6; surface exit/error (bug 6); bump version floor — `ec3e319` (see docs/acp-notes.md)
- [x] **1.4** non-destructive dashboard lifecycle + kill spawned child on quit (bug 7) — `d1ae837`
- [x] **3.2** wire Cowork composer to Cowork session (bug 2) — `197a3ff` (pause/resume/retry still TODO)
- [x] **0.3** `docs/security-model.md` + folder-scope guard + cwd validation — this commit
- [ ] Phase 2 (projects): project-store, create/switch, safe folder picker, project context, file browser

## Live-run findings (2026-09-01)
- `ELECTRON_RUN_AS_NODE=1` is set in the dev shell → Electron boots as Node and crashes on `app.whenReady`. Launch with `env -u ELECTRON_RUN_AS_NODE pnpm dev`. (Task 0.1 follow-up: unset it in the dev script?)
- Dashboard API shapes did not match the renderer's Zod schemas → "Could not reach Hermes". Fixed profiles / active / status / switch / sessions in `9104bdc`, verified against a live 0.20.6 dashboard.
- Still to remap: `/api/plugins/kanban/board` returns `{ columns: [{name, tasks}] }`; `createKanbanTask` body/response unverified.
- Dashboard reuse path confirmed working: app attached to the user's already-running `hermes dashboard` on :9119 (child=null, not killed on quit).

## Cleanup pass (2026-09-01)
- Deleted dead IPC: `runtime:status`, `runtime:rescan`, `profile:list` (renderer reads these straight from the dashboard via the REST proxy). Removed unused `StatusSnapshot` / `ProfileSummary` from shared/types.
- Removed unused `api.kanbanBoard()` / `api.createKanbanTask()` from rest-client — re-add correctly in the Cowork phase.
- `KanbanTaskSchema` remapped to real 0.20.6 shape (string ids, 8-state enum, unix `created_at`, `link_counts`). `parentTaskId` is now `string | null`. `KanbanEventSchema` still unverified — FIXME left.
- Chat no longer passes `cwd: '/'`; `acp:start` defaults an absent cwd to homedir (chat is not folder-scoped; Cowork still requires an existing picked folder).
- NewTaskDialog profile is a `<select>` of real profiles, defaulting to the active one.

## Cowork slice (2026-09-01, `9e06bb2`)
Discovered the old Cowork design doesn't match Hermes 0.20.6: agent ignores `kanban_create` prompts, no ACP `plan` updates, kanban events WS needs an unminted auth ticket (`POST /api/auth/ws-ticket`).
- Rebuilt: kickoff asks for a numbered plan + STOP (verified); PlanTab parses steps from the transcript and shows an Approve gate.
- Approval mode enforced via ACP `session/set_mode` (`default`/`accept_edits`) — verified on 0.20.6. `session/cancel` does NOT exist on 0.20.6.
- Cowork sessions run isolated (own ACP child) so Stop hard-kills; chat stays pooled.
- Artifact detection now on tool-call `kind` (edit/delete/move) + path arg.
- KanbanWsPump not started (note in index.ts).

### Cowork UI fixes (`124fd0c`)
- Sidebar used raw `<a href>` → full-page nav → blank screen on `/cowork/new`. Now wouter `<Link>`.
- NewTaskDialog awaited the whole prompt turn before navigating → all plan tokens emitted before CoworkPage's listener mounted → transcript stuck. Kickoff now stashed in store; CoworkPage registers listener then sends.
- `tests/e2e/cowork-flow.spec.ts` drives the full flow vs real Hermes (plan → approve → inline edit approval → file on disk). Auto-skips without Hermes.
- Verified: plan-then-approve, ACP mode enforcement, isolated child + Stop, inline approvals, artifact write all work through the UI.

### Cowork still open
- Plan step completion tracking (no per-step status — the list is static).
- `planTasks` / `upsertPlanTask` / `setParent` / `KanbanTask*` now unused (kept for Phase 5 subtasks).
- Live kanban view needs the ws-ticket auth flow.
- Not tested through the actual UI yet (probed at ACP layer only).

## Still open from touched tasks
- **1.3**: tool_call / request_permission / cancel not yet exercised against 0.20.6 (trivial prompt); confirm with a tool-triggering prompt.
- **3.2**: pause / resume / retry.
- **0.3**: approval expiry timer; `resolveWithinRoot` not yet wired into a file browser (Task 2.5); approval-mode still renderer-only (Task 3.3).
- **bug 5** (artifact detection: store matches tool id, translator emits title) — Task 3.5, untouched.

Commit at meaningful checkpoints.
